function soma(x = 1, y = 1) {
    const resultado = x + y;
    return resultado;
}

const resultado = soma(4, 2);
console.log(resultado);

const raiz = function (n) {
    return Math.sqrt(n);
};

console.log(raiz(8));
console.log(raiz(9));