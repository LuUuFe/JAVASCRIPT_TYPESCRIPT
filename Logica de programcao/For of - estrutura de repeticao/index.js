/* const nomes = ['Luis', 'Felipe', 'Henrique'];

for (let i = 0; i < nomes.length; i++) {
    console.log(nomes[i]);
}

console.log('************************************')

for (let i in nomes) {
    console.log(nomes[i]);
}

console.log('************************************')

for (let valor of nomes) {
    console.log(valor)
}

console.log('************************************')

nomes.forEach(function(valor, indice, array) {
    console.log(valor, indice, array)
}); */

// For clássico - Geralmente com interáveis (array ou strings)
// For in clássico - Geralmente com interáveis (array, strings ou objetos)
// For of clássico - Geralmente com interáveis (iteráveis, array ou strings)

const pessoa = {
    nome: 'Luis',
    sobrenome: 'Felipe'
};